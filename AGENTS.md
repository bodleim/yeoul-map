# 프로젝트 작업 가이드

## 서비스 목적과 범위

- Next.js App Router + TypeScript + Tailwind CSS로 만드는 행사 부스 메뉴 안내 웹이다.
- 직사각형 잔디밭의 왼쪽에 8개, 오른쪽에 8개, **총 16개 부스**가 있다.
- 핵심 흐름은 `지도(/) → 부스 선택 → 해당 부스 메뉴(/booths/[id]) → 지도로 돌아가기`다.
- 방문자가 휴대폰에서 부스를 찾고 메뉴를 읽기 쉽게 만든다.
- 최종 지도 디자인 이미지는 나중에 제공된다. 이미지가 제공되기 전에는 부스 목록 링크를 기본 화면으로 사용한다.
- 별도 요청 전에는 로그인, 주문, 결제, 관리자 화면, DB, 지도 SDK, 검색·필터 기능을 추가하지 않는다.

## 작업 방식

1. 사용자 요청과 이 문서를 읽고, 관련 파일의 현재 구현부터 확인한다.
2. 코드 작성 전 아래 자동 생성 안내에 따라 설치된 Next.js의 관련 문서를 읽는다. 기억에 의존해 API를 바꾸지 않는다.
3. 요청에 필요한 최소 범위를 수정한다. 문서 작성만 요청받았다면 앱 코드를 수정하지 않는다.
4. 기존 파일과 컴포넌트를 우선 사용한다. 필요 없는 라이브러리, 공통화, 폴더 계층을 만들지 않는다.
5. 변경 후 관련 화면과 빌드·린트를 확인하고, 변경 내용과 검증 결과를 짧게 보고한다. 실행하지 않은 검증은 통과했다고 쓰지 않는다.

## 현재 파일과 역할

| 파일 | 역할 |
| --- | --- |
| `app/page.tsx` | 부스별 메뉴 링크를 제공하는 기본 화면; 최종 지도 디자인의 적용 위치 |
| `app/booths/[id]/page.tsx` | 부스 ID 검증과 해당 부스 메뉴 표시 |
| `data/menus.ts` | 부스별 메뉴 데이터 |
| `app/layout.tsx` | 공통 레이아웃, 메타데이터, 뷰포트 |
| `app/globals.css` | Tailwind 진입점과 전역 스타일 |

**현재 구현 기준:** 지도, 정적 경로 생성, ID 검증, 메뉴 데이터는 모두 16개 부스를 기준으로 작성되어 있다. 부스 수나 배치를 수정하는 작업에서는 이 네 부분을 함께 확인한다. 문서를 추가하는 것만으로 구현이 바뀐 것으로 간주하지 않는다.

## 부스와 메뉴 데이터

- 최종 배치가 오기 전 기본 번호는 왼쪽 위→아래 `1~8`, 오른쪽 위→아래 `9~16`으로 한다. 실제 행사 번호가 제공되면 그 배치를 따른다.
- 부스 ID는 URL과 메뉴 데이터를 연결하는 고유 값이다. 배열 순서나 부스 이름으로 메뉴를 찾지 않는다.
- 부스 수나 배치를 정리할 때는 `data/booths.ts`에 부스 목록을 두고 지도 링크, 유효 ID 판정, 정적 경로 생성을 이 목록에서 파생한다. 여러 파일에 `16`이나 번호 배열을 반복 작성하지 않는다.
- 현재 부스 목록에는 필요한 값만 둔다: `id`, 표시 이름. 최종 지도 디자인을 적용할 때만 좌우 구분과 지도 클릭 영역을 추가한다. 메뉴 데이터는 기존 `data/menus.ts`를 우선 활용한다.
- 타입을 명시하고 `any`로 오류를 덮지 않는다. 타입 단언만으로 모든 부스 데이터가 존재한다고 가정하지 않는다.
- 부스는 존재하지만 메뉴가 비어 있으면 `메뉴 준비 중입니다`를 표시한다. 존재하지 않는 부스 ID는 `notFound()`로 처리한다.
- 실제 부스명·메뉴·가격이 없으면 임시 데이터임을 명확히 한다. 임의로 만든 내용을 확정 정보처럼 표시하지 않는다.

## 지도 구현 원칙

- 단순한 이미지와 링크로 구현한다. 지도 SDK, Canvas, 확대·이동 엔진은 필요하지 않다.
- 이미지와 클릭 영역은 **동일한 `position: relative` 컨테이너** 안에 둔다. 컨테이너 비율은 이미지 원본 비율과 일치시킨다.
- 각 클릭 영역은 `position: absolute`로 배치하고 `left`, `top`, `width`, `height`를 이미지 기준 **0~100 퍼센트 값**으로 관리한다. 화면 전체나 고정 픽셀 좌표를 기준으로 잡지 않는다.
- 좌표는 부스 데이터 한 곳에 모은다. JSX 여러 곳에서 별도 계산식을 사용하거나 좌우 각각 부스 수를 하드코딩하지 않는다.
- 이미지 전체가 보이도록 원본 비율을 유지한다. `object-cover`로 이미지를 잘라 클릭 위치가 어긋나게 만들지 않는다.
- 최종 이미지가 도착하면 원본 비율과 16개 클릭 영역을 함께 조정한다. 기존 이미지의 좌표가 그대로 맞는다고 가정하지 않는다.
- 최종 이미지 전에는 클릭 가능한 부스 번호 목록을 기본 화면으로 사용한다. 임시 이미지를 추가하지 않는다.
- 지도 링크에는 부스 번호·이름을 포함한 접근 가능한 이름을 준다. 키보드 포커스가 보여야 하며, 링크 전체에 `opacity: 0`을 적용해 포커스까지 숨기지 않는다.
- 좁은 화면에서 지도를 지나치게 축소하지 않는다. 터치 영역은 가능하면 44×44 CSS px 이상 확보하되 이웃 부스와 겹치지 않게 한다. 확보하기 어려우면 같은 부스로 이동하는 번호 목록을 함께 제공한다.

## 페이지와 스타일 구현 원칙

- 내부 페이지 이동은 `next/link`를 사용한다. 메뉴 화면에는 부스 번호·이름, 메뉴명, 가격, `지도로 돌아가기` 링크를 표시한다.
- 메뉴 URL을 직접 열거나 새로고침해도 정상적으로 표시되어야 한다. 이전 화면의 상태에 의존하지 않는다.
- 기본은 Server Component다. 클릭 후 페이지 이동만 하는 링크에 `"use client"`, `useState`, `useEffect`를 추가하지 않는다.
- `params` 등 Next.js API는 설치 버전의 문서를 따른다. 현재 메뉴 페이지의 비동기 `params`를 과거 예제에 맞춰 임의로 변경하지 않는다.
- Tailwind 유틸리티를 우선 사용하고 전역 CSS는 공통 규칙에 한정한다. 데이터에서 오는 퍼센트 좌표는 인라인 스타일을 사용해도 된다.
- 현재 Tailwind 진입점은 `@import "tailwindcss";`다. 구버전 설정 파일이나 지시문을 관성적으로 추가하지 않는다.
- 모바일을 먼저 맞추고 데스크톱에서 최대 너비를 제한한다. 긴 메뉴명과 가격이 겹치지 않게 하며 브라우저 확대를 막지 않는다.
- 기존 이미지에는 `next/image`를 우선 활용하고 크기·비율·대체 텍스트를 지정한다. `fill` 사용 시 부모의 크기와 위치 기준을 명확히 한다.

## 구현 완료 기준

변경에 해당하는 항목을 확인한다. 관련 없는 기능까지 새로 구현하지 않는다.

- [ ] 지도 디자인 적용 전에는 16개 부스 링크가 표시되고 ID가 중복되지 않는다.
- [ ] 16개 링크가 각각 올바른 부스 메뉴로 이동한다. 지도 디자인 적용 후에는 이미지와 클릭 영역도 맞는다.
- [ ] 첫 부스·마지막 부스를 포함해 메뉴 URL 직접 접속과 새로고침이 된다.
- [ ] 없는 부스 ID는 404, 빈 메뉴는 준비 중 안내로 처리된다.
- [ ] 메뉴 화면에서 지도로 돌아갈 수 있다.
- [ ] 360px 너비의 모바일 화면과 데스크톱에서 가로 넘침, 텍스트 겹침, 클릭 영역 겹침이 없다.
- [ ] 키보드 Tab과 Enter로 부스를 선택할 수 있고 현재 포커스가 보인다.
- [ ] 코드 변경 시 `npm run lint`, `npm run build`를 실행한다. 실패하면 원인을 확인하고 기존 문제와 이번 변경으로 생긴 문제를 구분한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
